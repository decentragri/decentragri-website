export interface FarmData {
  id: string;
  name: string;
  location: string;
  size: number;
  cropType: string;
  description?: string;
  imageUrl?: string;
  createdAt?: string;
  updatedAt?: string;
}
